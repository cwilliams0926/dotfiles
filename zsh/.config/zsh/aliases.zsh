#!/usr/bin/env zsh

alias psmouse="sudo modprobe -r psmouse && sudo modprobe psmouse"
alias ls="eza -l"
alias grep="grep -P -i --color=auto"

alias cp="cp -iv"
alias mv="mv -iv"
alias rm="rm -iv"

alias vim="nvim"
alias vi="nvim"
alias code="codium"
